# Copy these values into Streamlit Cloud → App settings → Secrets
# The app also works without OPENAI_API_KEY (rule-based fallback analyzer).

OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-4o-mini"
LOG_LEVEL = "INFO"
EMBEDDING_MODEL = "all-MiniLM-L6-v2"
